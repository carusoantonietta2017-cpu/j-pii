{
  "name": "ocr-pi-ui",
  "version": "0.1.0",
  "description": "Backend locale PWA ocr-pi (statici + REST + SSE dock). SDK solo per il dock.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test server.test.mjs dock.test.mjs"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "typebox": "1.3.7"
  }
}
